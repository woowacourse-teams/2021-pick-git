spring:
  data:
    web:
      pageable:
        size-parameter: limit
  profiles:
    include: security
  flyway:
    enabled: false
server:
  compression:
    enabled: true
    mime-types: application/json
